main:
    ;call lcd_init
    mov 1, board[0] ; X__
    mov 0, board[1] ; ___
    mov 0, board[2] ; ___
    mov 0, board[3]
    mov 0, board[4]
    mov 0, board[5]
    mov 0, board[6]
    mov 0, board[7]
    mov 0, board[8]

    push 0
    push 2
    push board.l
    push board.h
    call tictactoe_minimax
    addi SP, 4, SP
    mov *choice, B
    halt

board = 10000200h
;#include "lcd.rcpu"
#include "tictactoe.rcpu"
